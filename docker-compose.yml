services:
  newsletter:
    build:
      context: .
      dockerfile: Dockerfile
    image: rss-ai-newsletter:latest
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: "no"
